<%= simple_form_for @position do |f| %>
  <%= f.input :name %>
  <%= f.input :department_id,:collection => current_company.departments %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
